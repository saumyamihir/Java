import java.util.*;

public class array {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
 /* 
        // Size of Array
        System.out.println("Enter the size of Array :");
        int n = sc.nextInt();

        // Declaration of the array
        int[] arr = new int[n];

        for (int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();
        }

        // Print the array
        System.out.print("Array: ");
        for (int i = 0; i < n; i++) {
            System.out.print(arr[i] + " ");
        }

        //Reverse the array using while loop
        int low = 0;
        int high = n-1;
        while (low <= high)
        {
           //Swap arr[low] to arr[high] using temp variable
           int temp = arr[low];
           arr[low] = arr[high];
           arr[high]= temp;

           //Incearse low and decrease high
           low++;
           high--;
        }  
        System.out.println();
        System.out.print("Reverse array :");
        for(int i=0;i<n;i++)
        {
            System.out.print(arr[i]+" ");
        }
    



     /*  Two Sum Problem 
     11 20 3 40 25 60 13
     11+20=31
     11+3=13
     11+40=51
     .......
     40+60=100 maximum sum
     Use two for loop

     
    int max_sum=0;

    for(int i=0;i<n-1;i++)
    {
        for(int j=i+1;j<n;j++)
        {
            int sum=arr[i]+arr[j];
            if(sum>max_sum)
            {
                max_sum =sum;
            }
        }
    }
    System.out.println();
    System.out.println("Maximum value is :"+max_sum);
    
*/


    
}
}



